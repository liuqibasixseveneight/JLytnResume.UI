import styled from 'styled-components';

export const ProjectTagsWrapper = styled.div`
  display: flex;
  font-size: 1.4rem;
  font-weight: 500;
  gap: 0.5rem;
  text-transform: uppercase;
`;

export const ProjectTagSeparator = styled.span`
  font-weight: 700;
`;
